{
  "name": "hospital-appointment-system",
  "version": "1.0.0",
  "description": "A full-stack hospital appointment management system",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install && npm start",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "install-deps": "cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["hospital", "appointment", "healthcare", "react", "nodejs"],
  "author": "Hospital Appointment System",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
